name: Tests

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Setup Mamba
        run: |
          curl -L -o miniforge.sh "https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-$(uname)-$(uname -m).sh"
          bash miniforge.sh -b -p $HOME/miniforge
          conda create -y -c conda-forge -n freecad python=3.11 freecad=0.21.2
          conda activate freecad
          python -m pip install --upgrade pip
          pip install -e .
          pip install -e .[dev]
          python -m pytest -v

        # uses: mamba-org/setup-micromamba@v2
        # with:
        #   environment-name: freecad
        #   create-args: >-
        #     python=3.11
        #     freecad=0.21.2
